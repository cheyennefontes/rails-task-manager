<h1>Task info</h1>
<h2>Title:<%=@task.title%></h2>
<p>Details:<%=@task.details%></p>

<%= link_to 'Delete', task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are sure you?"} %>
<%= link_to 'Back', tasks_path %>
